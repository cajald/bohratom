# bohratom

A Bohr's Model display renderer for LaTeX, based on TikZ. See more documentation by cloning and running `make` to generate a `main.pdf` with docs.

Under the public domain, by Mario Rosell.
